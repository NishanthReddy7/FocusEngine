{
  "name": "@focusengine/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "FocusEngine Android shell — Capacitor wrapper around the mobile-flavored static web export (apps/web/out, NEXT_PUBLIC_PLATFORM=mobile).",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "assets": "capacitor-assets generate --android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/app": "^8.1.0",
    "@capacitor/browser": "^8.0.3",
    "@capacitor/core": "^8.4.1",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/status-bar": "^8.0.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.1"
  }
}
